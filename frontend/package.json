{
  "name": "goal-tracker-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0", // Upgraded to latest React version
    "react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^3.1.0", // Modern plugin for React with Vite
    "vite": "^4.0.0" // Vite as the build tool
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "vitest", // Replace Jest with Vitest if used
    "preview": "vite preview"
  },
  "devDependencies": {
    "eslint": "^8.40.0", // Modern linting support
    "eslint-plugin-react": "^7.32.0"
  },
  "eslintConfig": {
    "extends": [
      "plugin:react/recommended"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
